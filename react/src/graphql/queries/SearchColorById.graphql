query searchColorById($id: String) {
  searchColorById(id: $id) {
    data {
      color {
        colorName
        creationDate
        value
        type
        variations
        sellers
        id
        isLight
      }
    }
  }
}
