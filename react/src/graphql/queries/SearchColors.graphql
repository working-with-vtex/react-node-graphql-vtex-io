query searchColors($filter: String) {
  searchColors(filter: $filter) {
    data {
      colors {
        colorName
        creationDate
        value
        type
        variations
        sellers
        id
        isLight
      }
    }
  }
}
