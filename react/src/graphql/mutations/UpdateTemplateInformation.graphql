mutation updateColorsTemplateInformation(
  $template: ColorsTemplateInformationUpdateInput!
  $acronym: String!
) {
  updateColorsTemplateInformation(template: $template, acronym: $acronym) {
    status
    message
  }
}
